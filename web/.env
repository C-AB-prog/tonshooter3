# Web env (apps/web)

# Если используешь Vite proxy (/api -> API), можно НЕ задавать VITE_API_URL.
# Тогда фронт сам будет ходить в /api
# VITE_API_URL=https://YOUR_API_PUBLIC_URL/api

# Payment mode: mock (тестовый) or tonconnect (боевой)
VITE_TON_PAY_MODE=mock

# TonConnect manifest URL (нужно только если включишь tonconnect)
VITE_TONCONNECT_MANIFEST_URL=

VITE_BOT_USERNAME=tonshooter_bot
